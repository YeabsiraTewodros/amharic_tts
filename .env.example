## Example environment variables for Amharic TTS server
# Do NOT commit your real credentials. Copy this to `.env` locally and
# set the actual password there, then ensure `.env` is in `.gitignore`.

# PostgreSQL example:
# Replace user/host/port/dbname and set your password in your real `.env`.
# DATABASE_URL=postgresql://youruser:yourpassword@localhost:5432/yourdb

# If unset, the app will fall back to a local SQLite file `amharic_tts.db`.
